{
  "name": "story-image-runner-plugin",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "Chrome/Edge extension for queued batch image generation through a signed-in ChatGPT web session.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check-extension.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
